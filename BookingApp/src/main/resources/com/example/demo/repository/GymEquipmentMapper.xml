<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.GymEquipmentMapper">

    <!-- 結果マッピング -->
    <resultMap id="GymEquipmentResultMap" type="com.example.demo.domain.model.GymEquipment">
        <id property="id" column="id" />
        <result property="siteId" column="facility_type_id" />
        <result property="equipmentName" column="description" />
        <result property="isInUse" column="is_in_use" javaType="boolean"/>
    </resultMap>

    <!-- 現在のジム機器の利用状況を取得（is_active を利用中フラグに） -->
<select id="findCurrentGymEquipmentStatus" resultMap="GymEquipmentResultMap">
    SELECT 
        f.id,
        f.facility_type_id,
        f.description,
        CASE 
            WHEN f.is_active = 0 THEN TRUE 
            ELSE FALSE 
        END AS is_in_use
    FROM facilities f
    WHERE f.facility_type_id = 2
    ORDER BY f.id
</select>


    <!-- 全ジム機器の一覧を取得（利用状況は常にfalse） -->
    <select id="findAll" resultMap="GymEquipmentResultMap">
        SELECT 
            id,
            facility_type_id,
            description,
            FALSE AS is_in_use
        FROM facilities
        WHERE facility_type_id = 2
        ORDER BY id
    </select>
    
    <select id="findById" resultMap="GymEquipmentResultMap">
  SELECT id, facility_type_id, description, CASE WHEN is_active = 0 THEN TRUE ELSE FALSE END AS is_in_use
  FROM facilities
  WHERE id = #{id}
</select>

<update id="updateUseStatus">
  UPDATE facilities
  SET is_active = CASE WHEN #{isInUse} THEN 0 ELSE 1 END
  WHERE id = #{id}
</update>
    

</mapper>
